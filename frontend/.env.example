# Copy to .env.local and fill in your own values. .env.local stays private.
# Paste your Google Maps JS API key after the = below.
VITE_GOOGLE_MAPS_API_KEY=
# Active map provider: maplibre (default) or google.
VITE_MAP_PROVIDER=maplibre
